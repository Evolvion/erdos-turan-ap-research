# Artifact Specs

- (V3-δ,η), (V4-δ,η) residual checks implemented in `inequalities.py`.
- √T₃ pruning in `prune.py` (returns deletion sets and final mask).
- ILP/WCNF emitters in `encodings.py` with exact clause counts.
- Minimal CLI in `cli.py` for reproduction and self-tests.
